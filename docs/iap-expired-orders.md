# 到期订单解除购买限制

后端提交 `feea831`，客户端提交 `528f0b1`，客户端里程碑 41（3.1.1+185）。

覆盖后端更新 ZIP 中的 backend 文件，保留服务器已有 `.env`，按原方式重启即可。
本包累计包含自动续订、启动建表、URL 计划任务及本次修复。无需改变通知地址、任务地址或运行建表命令。
保留上次修正后的完整 HUAWEI_IAP_KEY_ID（末尾带 a），本包不包含环境变量或私钥。

客户端恢复旧订单时，后端先验证凭证签名，再向华为查询最新订阅并核对所属用户。
已失效返回 HTTP 410 / subscription_inactive（包含请求订单和商品 ID）；客户端仅移除该笔待验证订单，重新允许购买，不发会员、不确认发货。
已续期则恢复最新会员；查询超时、账号冲突、凭证无效均不会被误判为到期。
旧后端不会返回该明确结果，所以必须配套更新；不要把所有 409/422 当作到期。

安装客户端并更新后端后，点“恢复购买”。已确认失效的订单应显示“原订阅已失效，可重新开通月会员”，购买按钮可用。
测试覆盖重复恢复、旧凭证恢复新一期、失效不发权益、查询失败仍可重试、无关订单响应不解除限制。
本地后端 38 项、客户端 7 项通过。真实设备到期链路需在服务器更新后验证。

后端包：`build/backend-milestones/04-expired-orders/ucenter-iap-expired-orders-update.zip`。
客户端包：`build/milestones/41-iap-expired-orders`。
