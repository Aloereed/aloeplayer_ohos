# 里程碑 94：系统投播就绪按钮刷新

版本：4.0.1+247。

2026-09-09 22:17 通过 HDC 获取当前设备截图：Flutter 已显示“点击下方选择投播设备”，原生自定义按钮仍灰色并显示“正在准备投播”。这证明 READY 已回传，但按钮显示未同步；不能据此判定接收端播放成功。

将加载状态移出 AVCastPicker.customPicker，在外层 ArkUI 条件分支中显示。只有 ready 后才创建系统选择器，并传入最终 sessionType；自定义按钮使用固定文字和颜色，避免依赖系统组件内部自定义构建器刷新状态。加载阶段不创建可操作的系统选择器。

保留里程碑 93 的循环关闭修复及里程碑 92 的上游原版 MPV。

构建：./build.ps1 hap debug -Offline -Locked -NoVersionBump。调试签名、Flutter release 编译。

复测：进入系统投播，初始化完成后应显示蓝色“选择投播设备”；点击应打开系统面板。退出后重新进入也应正常。需真机验证本次改动及实际接收端播放；构建通过不能替代设备验证。
