# 里程碑 78：剧集连播与有界进度同步（4.0.1+231）

媒体库详情和历史记录继续播放入口支持上一集、下一集和默认开启的自动连播。播放器设置可关闭自动连播；单集/列表循环优先于自动切集，“播完当前集停止”不会继续下一集。首尾不循环，按服务器剧集顺序跨季；旧服务器忽略 AdjacentTo 时按 200 项分页查找，并在重复页停止。

下一集只在需要切换时准备，不提前建立整季转码会话。保留画质、播放模式和明确的关闭字幕设置；新一集使用自身默认音轨/字幕，不复用旧 MediaSourceId 或轨道数字。前进从头播放，后退采用服务器续播位置；切集清除上一集的 A-B 循环。准备失败保留当前集并允许重试，退出时取消准备请求，未进入播放的新媒体源释放时不写入虚假的零位置观看记录。

进度上报由独立会话队列处理：最多一条正在发送、一条最新待发，终止状态不会被迟到进度覆盖。慢会话不阻塞新会话；每次上报 6 秒截止，关闭等待最多 5 秒后取消请求并清理媒体源。开始确认失败时仍尝试停止通知。网络完全不可达时不能保证服务器收到最终位置，本阶段未加入持久离线上报箱。

验证覆盖：10,000 次更新合并、旧请求失败后的新位置恢复、独立会话互不阻塞、开始失败后停止与通道释放、跨页跨季/首尾/重复页、下一集源与轨道隔离、准备后丢弃再重试、媒体页面生命周期。真实 Jellyfin 12.0.0 和 Emby 4.10.0.40 验证前后集边界及原画/转码/字幕/音轨/续播/收藏链路。

Emby 测试副本的可选 DLNA 插件移到 build/media-server-fixtures/disabled-plugins，保留备份；核实 1900 监听消失，仅剩 7359 服务发现监听，仍保留仅针对该测试程序的 TCP/UDP 入站阻止规则。测试仅访问本机，没有访问用户设备或媒体服务器。

鸿蒙播放器实际切集渲染、后台音频和屏幕锁交互尚待设备部署验证；桌面真实服务测试不能替代这些实机行为。MPV 未重新编译。debug HAP 与 release APP 分别归档，下一阶段继续推进离线与筛选等体验。
