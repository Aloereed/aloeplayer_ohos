# 里程碑 80：可交付的下载与离线观看（4.0.1+233）

包含里程碑 79 的原文件下载、断点续传、后台队列、下载页离线观看和历史记录优先使用本地完整副本，并补齐服务器账号下载权限检查。每次准备或恢复下载均读取当前用户策略，EnableContentDownloading=false 时在请求媒体源之前拒绝下载。媒体级 CanDownload=false 仍受尊重。权限、登录和存储错误显示简洁中文说明，不直接显示底层请求异常。

真实 Jellyfin 12.0.0 和 Emby 4.10.0.40 使用本机生成测试账号验证：临时禁止下载后请求被拒绝；恢复原策略后完整原文件和从第 4096 字节续传的 SHA-256 与源文件一致。测试通过后原策略恢复，Emby 测试进程停止。另有故障服务器用例确认禁止下载时不调用 PlaybackInfo。

79 已保留为不可部署的内部候选，文件不覆盖、不删除。安装包索引不提供其部署链接，部署脚本跳过或拒绝该候选；回退应选择 78。80 才是本批下载功能的可交付包。

Emby 启动阶段可能暂时返回 503，测试启动器增加就绪检查；没有把启动暂态当作播放器兼容成功。全部测试只访问本机生成服务器，没有访问用户设备。

仍需推进：外置字幕随视频下载、离线观看进度补报、整季批量下载，以及更丰富的媒体筛选/音乐体验。本机测试不替代鸿蒙实机上的后台下载与切集验收。MPV 未重新编译。
