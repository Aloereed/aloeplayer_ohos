# WebDAV / SMB 夜间专项里程碑

开始：2026-09-09 01:29（北京时间）；工作预算 6 小时，预计最迟 07:29 收尾。

目标：优先修复 SMB 服务器根目录共享枚举及进入共享，随后完善 WebDAV 路径、响应解析与读取，再检查网络媒体浏览、播放、下载与资源生命周期。

## 交付计划

1. 54：SMB 根目录、共享导航及地址规范化；回归测试与独立 HAP。
2. 55：WebDAV 编码、目录、认证和范围读取兼容性；本机 HTTP 测试与独立 HAP。
3. 56 及后续：根据测试发现完善流读取、并发、取消、错误反馈与媒体库集成；每个稳定检查点单独提交、打包。

每个完成里程碑在此记录提交、版本、验证范围及安装顺序。独立产物放在 `build/milestones/`，记录 SHA-256 与 canonical HAP 时间戳。由用户明早从新到旧自行部署。

## 验证边界

- 本次不访问、探查、安装或操作用户鸿蒙设备。
- 所有 Flutter / Hvigor / OHOS 构建在沙箱外执行。
- 不将模拟测试、编译成功描述为 NAS 或设备实测；性能改进说明机制及可测证据。
- 起始工作区已有 `.vscode/settings.json`、两个 OHPM lock 文件、`test/credential_migration_test.dart` 和 `output/` 未提交变化；保留，不混入专项提交。

## 初始发现

- SMB 只连接 IPC$ 并返回两条虚假文件提示，未实现共享枚举。
- SMB 地址仅按斜杠拆分，不能正确识别 smb://、UNC 等输入。
- WebDAV 多状态解析依赖固定命名空间前缀，路径处理混用解码路径与 URL，缺乏有效 propstat 筛选。

## 已完成

### 54 — SMB 服务器根目录与共享导航（4.0.0+207）

- 接入原生 SRVSVC Level 1 共享枚举，过滤打印机/IPC，保留可授权访问的隐藏磁盘共享；删除虚假文件提示。
- 服务器根目录下的共享可直接进入；目录、stat、播放路径统一路由到所属共享。每个共享持有独立连接，最多保留八个数据共享连接，淘汰时避开活动读取器。
- 支持裸主机、SMB URL、UNC、端口、IPv6 和嵌套起始目录；URL 解码仅发生在地址边界，保留文件名中的百分号、空格、井号和问号。
- stat 改用原生路径 stat，避免把目录当普通文件打开；连接错误保留具体原因；配置页说明服务器根目录与禁止枚举时的直连方法。
- 6 项专项测试通过，其中 C 测试夹具使用项目自带头文件，检查 FFI 结构体大小、共享过滤、错误回调释放、跨共享读取不串连接、连接池淘汰与嵌套根目录。静态分析无 error / warning。
- 原生测试夹具不连接 NAS；真实服务器认证策略、共享权限与设备播放仍待用户验收。构建与产物信息归档后补充。
