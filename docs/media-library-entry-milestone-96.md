# 里程碑 96：添加视频引导到媒体库

版本：4.0.1+249。

“添加视频”的旧“从 WebDAV 下载”改为“前往媒体库（WebDAV / SMB）”。点击先关闭添加面板，然后切换到主界面的媒体库标签，在统一入口管理网络媒体源、浏览和播放文件。移除视频页不再使用的旧 WebDAV 下载弹窗方法与导入。

保留本地复制、快捷方式、相册导入、文件管理器和直接播放入口。本次仅调整添加视频入口。

验证：运行已有 test/local_import_sheet_test.dart；构建 debug 签名 HAP（Flutter release 编译），核对 canonical HAP 时间戳及包内上游 MPV。

真机待测：添加视频 → 前往媒体库，应关闭面板并选中媒体库标签。
