# SMB媒体库改进指南

## 概述

全新改进的SMB媒体库提供了更美观的UI、更强大的功能和更好的用户体验。

## 主要改进

### 1. 多服务器配置管理
- **服务器列表页面** (`SmbServersPage`): 集中管理所有SMB服务器配置
- **功能**:
  - 添加/编辑/删除服务器配置
  - 为每个服务器设置名称、主机地址、用户名、密码、域和初始路径
  - 查看最后连接时间
  - 一键连接到已保存的服务器

### 2. 美化的文件浏览器
- **新的浏览器页面** (`SmbBrowserPageNew`): 全新设计的文件浏览界面
- **UI改进**:
  - 列表视图和网格视图切换
  - 更清晰的文件和文件夹图标
  - 视频文件特殊标记(红色播放图标)
  - 搜索高亮和结果过滤

### 3. 增强的功能
- **搜索**: 实时搜索文件和文件夹
- **排序**: 按名称、大小或修改时间排序,支持升序/降序
- **面包屑导航**: 清晰显示当前路径
- **返回历史**: 支持返回上级目录的导航历史

### 4. 视频播放集成
- **一键播放**: 点击视频文件直接使用MPVPlayer播放
- **支持格式**: mp4, mkv, avi, mov, flv, wmv, webm, m4v, ts, mpg, mpeg等
- **HTTP流媒体**: 自动通过HTTP服务提供视频流

## 文件结构

```
lib/
├── models/
│   └── smb_server_config.dart          # 服务器配置模型
├── services/
│   ├── smb_service.dart                # SMB服务
│   ├── smb_config_service.dart         # 配置管理服务
│   └── http_service.dart               # HTTP流媒体服务
├── pages/
│   ├── smb_servers_page.dart           # 服务器管理页面
│   ├── smb_browser_page_new.dart       # 新的文件浏览器
│   └── smb_browser_page.dart           # 旧的文件浏览器(保留)
└── mpvplayer.dart                      # 视频播放器
```

## 使用方法

### 1. 添加SMB服务器

1. 打开应用,导航到"SMB媒体库"页面
2. 点击"添加服务器"按钮
3. 填写以下信息:
   - **服务器名称**: 自定义名称(例如: "家庭NAS")
   - **主机地址**: SMB服务器IP地址(例如: 192.168.1.100)
   - **用户名**: SMB登录用户名
   - **密码**: SMB登录密码
   - **域**: (可选)域名,默认为WORKGROUP
   - **初始路径**: 连接后默认打开的路径(例如: /Bangumi)
4. 点击"保存"

### 2. 连接到服务器

1. 在服务器列表中,点击要连接的服务器卡片
2. 应用会自动连接并打开文件浏览器
3. 如果连接失败,请检查服务器配置是否正确

### 3. 浏览文件

**切换视图模式**:
- 点击顶部工具栏的网格/列表图标切换视图

**搜索文件**:
- 点击搜索图标
- 输入文件名进行实时搜索
- 点击X图标清除搜索

**排序文件**:
- 点击排序图标
- 选择排序方式(名称/大小/修改时间)
- 切换升序/降序

**导航**:
- 点击文件夹进入
- 点击返回按钮返回上级目录
- 系统返回键也可以返回上级

### 4. 播放视频

**方式一**: 直接点击视频文件,自动使用MPVPlayer播放

**方式二**:
1. 点击视频文件
2. 在弹出的选项中选择播放
3. 或复制HTTP链接在其他设备播放

### 5. 管理服务器配置

**编辑服务器**:
1. 在服务器卡片上点击三点菜单
2. 选择"编辑"
3. 修改配置并保存

**删除服务器**:
1. 在服务器卡片上点击三点菜单
2. 选择"删除"
3. 确认删除

## 技术细节

### 数据存储
- 使用`SharedPreferences`存储服务器配置
- 配置以JSON格式保存
- 密码明文存储在本地(建议未来加密)

### HTTP服务
- 自动启动HTTP服务器(端口8080+)
- 支持Range请求,适用于视频流媒体
- 支持CORS,可在局域网其他设备访问

### 视频播放
1. 点击视频文件时,获取SMB文件路径
2. HttpService生成HTTP URL
3. 将HTTP URL传递给MPVPlayer
4. MPVPlayer通过HTTP流播放视频

## 依赖项

新增依赖:
```yaml
uuid: ^4.0.0  # 用于生成唯一服务器ID
```

现有依赖:
- `smb_connect`: SMB协议支持
- `shelf` / `shelf_router`: HTTP服务器
- `shared_preferences`: 本地存储
- `media_kit`: 视频播放

## 兼容性说明

- 保留了旧的`smb_browser_page.dart`文件,确保向后兼容
- 新功能通过`SmbServersPage`和`SmbBrowserPageNew`实现
- 可以逐步迁移到新页面

## 下一步改进建议

1. **缩略图支持**: 为视频和图片文件显示缩略图
2. **收藏夹**: 添加文件夹收藏功能
3. **下载管理**: 支持下载文件到本地
4. **密码加密**: 加密存储服务器密码
5. **断线重连**: 自动重连断开的SMB连接
6. **多选操作**: 支持批量操作文件
7. **文件信息**: 显示更详细的文件信息(修改时间、权限等)

## 使用新页面替换旧页面

要在应用中使用新的SMB媒体库,将导航目标从`SmbBrowserPage`改为`SmbServersPage`:

```dart
// 旧代码
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SmbBrowserPage()),
);

// 新代码
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SmbServersPage()),
);
```

或者在主界面直接集成`SmbServersPage`作为SMB媒体库的入口。
